[project]
name = "thesis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "bcn-scraper",
    "jsonschema>=4.26.0",
    "jupyter>=1.1.1",
    "openai>=3.9.0",
    "pandas>=3.0.2",
    "pyarrow>=24.0.0",
    "python-dotenv>=1.2.3",
]

[tool.uv.sources]
bcn-scraper = { path = "../bcn-scraper", editable = true }
