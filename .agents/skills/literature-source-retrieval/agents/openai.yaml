interface:
  display_name: "Literature Source Retrieval"
  short_description: "Find and triage scholarly sources"
  default_prompt: "Use $literature-source-retrieval to find new sources for this literature review and classify them as Core, Peripheral, or Discarded."
policy:
  allow_implicit_invocation: true
